# ==========================================
# 1. KESCUELITA (Vite - Estático)
# ==========================================
server {
    listen 80;
    server_name kescuelita.kemarketing.mx;
    return 301 https://$host$request_uri; # Forzar HTTPS
}

server {
    listen 443 ssl; 
    server_name kescuelita.kemarketing.mx;

    # Certificados SSL
    ssl_certificate /etc/letsencrypt/live/kemarketing.mx/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kemarketing.mx/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf; 
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # AUMENTAR LÍMITE DE SUBIDA
    client_max_body_size 50M;

    root /var/www/html/keEscuela/ArtSchool/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # NUEVO BLOQUE: Redirigir /uploads al backend para servir imágenes
    location /uploads/ {
        proxy_pass http://localhost:3000/uploads/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
}

# ==========================================
# 2. KESERVICIOS (Vite - Estático)
# ==========================================
server {
    listen 80;
    server_name keservicios.kemarketing.mx;
    return 301 https://$host$request_uri; # Forzar HTTPS
}

server {
    listen 443 ssl;
    server_name keservicios.kemarketing.mx;

    # Certificados SSL
    ssl_certificate /etc/letsencrypt/live/kemarketing.mx/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kemarketing.mx/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf; 
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    client_max_body_size 50M;

    root /var/www/html/keservicios/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
    #Configuración del Backend
    location /api/ {
        # Nginx recibe la petición y se la pasa a tu backend localmente
        proxy_pass http://localhost:8081/api/; 
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# ==========================================
# 3. KEMARKETING PRINCIPAL (Next.js Puerto 3001)
# ==========================================
server {
    listen 80;
    server_name kemarketing.mx www.kemarketing.mx;
    return 301 https://$host$request_uri; # Forzar HTTPS
}

server {
    listen 443 ssl;
    server_name kemarketing.mx www.kemarketing.mx;

    # Certificados SSL
    ssl_certificate /etc/letsencrypt/live/kemarketing.mx/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/kemarketing.mx/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf; 
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        # AQUÍ APUNTAMOS AL PUERTO 3002 (Nuevo puerto para KeMarketing)
        proxy_pass http://localhost:3002; 
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
